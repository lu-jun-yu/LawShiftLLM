# T3.4 object_reallocated_reduce (客体重分配缩减)

## 基本信息

| 属性 | 值 |
|------|-----|
| 类型编号 | T3.4 |
| 名称 | object_reallocated_reduce |
| 标签 | NV (Non-Valid) |
| 源文件 | **不存在** |
| 状态 | 未实现 |

## 说明

在当前项目中，**没有找到对应的实现文件** `CONSTRUCT_object_reallocated_reduce.py`。

## 预期功能描述

基于项目的命名规范和其他类型的处理逻辑，T3.4 应该具有以下特征：

### 目标
修改案件事实中的**犯罪客体**（Object）描述，通过"重分配"（reallocated）方式进行"缩减"（reduce）。

### 可能的实现方式
参考 Subject 和 Action 类型的实现，Object 类型可能：
- 修改犯罪对象的描述（如财物、金额等）
- 改变犯罪客体的性质或范围
- 使用正则表达式或LLM进行文本替换

### "重分配缩减"的含义
- **重分配（Reallocated）**：将原有描述重新分配到不同的类别
- **缩减（Reduce）**：减少或缩小犯罪客体的范围/价值

## 与其他类型的关系

| 类型 | 状态 | 说明 |
|------|------|------|
| T3.1 object_explicit_extend | V (Valid) | 客体显式扩展 |
| T3.2 object_implicit_expand | V (Valid) | 客体隐式扩展 |
| T3.3 object_reallocated_expand | V (Valid) | 客体重分配扩展 |
| **T3.4 object_reallocated_reduce** | **NV (未实现)** | 客体重分配缩减 |
| T3.5 object_explicit_reduce | NV (未实现) | 客体显式缩减 |
| T3.6 object_implicit_reduce | NV (未实现) | 客体隐式缩减 |

## 可能的原因

1. **功能尚未开发**：项目可能处于开发阶段，该功能计划后续实现
2. **设计决策**：可能认为 Object 类型的 reduce 修订在法律场景中不常见
3. **合并处理**：可能与其他类型合并处理，无需单独实现

## 建议

如需实现此功能，可参考以下模板：

```python
# CONSTRUCT_object_reallocated_reduce.py
import re
from file_operations import get_json_content, set_json_file

POISONED_OBJECT = "某物品"  # 替换后的客体描述

def split_factContent_by_pattern(str):
    """提取犯罪客体信息"""
    object_map = {}
    # 添加识别犯罪客体的正则表达式
    return object_map

def need_factContent_poisoned_fact(str):
    """处理客体信息并返回修改后的文本"""
    object_map = split_factContent_by_pattern(str)
    for key, value in object_map.items():
        str = str.replace(key, POISONED_OBJECT)
    return str, len(object_map) > 0

def main():
    # 数据处理流程
    pass
```
