# T3.5 object_explicit_reduce (客体显式缩减)

## 基本信息

| 属性 | 值 |
|------|-----|
| 类型编号 | T3.5 |
| 名称 | object_explicit_reduce |
| 标签 | NV (Non-Valid) |
| 源文件 | **不存在** |
| 状态 | 未实现 |

## 说明

在当前项目中，**没有找到对应的实现文件** `CONSTRUCT_object_explicit_reduce.py`。

## 预期功能描述

基于项目的命名规范和其他类型的处理逻辑，T3.5 应该具有以下特征：

### 目标
修改案件事实中的**犯罪客体**（Object）描述，通过"显式"（explicit）方式进行"缩减"（reduce）。

### "显式缩减"的含义
参考 T1.5 subject_explicit_reduce 的实现：
- **显式（Explicit）**：使用明确、直接的筛选或替换规则
- **缩减（Reduce）**：减少或缩小犯罪客体的描述范围

### 可能的实现方式
```python
def need_factContent_poisoned_fact(str):
    """
    检查是否包含特定客体关键词
    返回布尔值，用于筛选
    """
    object_map = split_factContent_by_pattern(str)

    for key, value in object_map.items():
        if value == "object":
            if key.find("财物") != -1 or key.find("现金") != -1:
                return True
    return False
```

## 与其他类型的关系

| 类型 | 修改方式 | 特点 |
|------|---------|------|
| T3.4 object_reallocated_reduce | 重分配 | 将客体重新分类 |
| **T3.5 object_explicit_reduce** | 显式 | 明确的筛选/替换规则 |
| T3.6 object_implicit_reduce | 隐式 | 间接影响客体描述 |

## 预期应用场景

### 可能的目标法条
- 盗窃罪（涉及财物价值）
- 诈骗罪（涉及金额认定）
- 侵占罪（涉及财产描述）

### 可能的修改内容
- 财物描述的模糊化
- 金额的缩减
- 价值认定的改变

## 为何标记为NV

即使未实现，基于命名规则，此类型应标记为NV的原因：
- "缩减"操作会改变犯罪客体的实质
- 可能影响案件的定性或量刑
- 使原本符合构成要件的事实变得不符合

## 建议实现模板

```python
# CONSTRUCT_object_explicit_reduce.py
import re
from file_operations import get_json_content, set_json_file

# 显式替换规则
EXPLICIT_RULES = {
    "巨额": "少量",
    "大量": "少量",
    "贵重": "普通"
}

def explicit_reduce_object(str):
    """
    显式替换客体描述
    """
    for old, new in EXPLICIT_RULES.items():
        if old in str:
            str = str.replace(old, new)
            return str, True
    return str, False

def main():
    # 数据处理流程
    pass
```
